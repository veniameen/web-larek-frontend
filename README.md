https://github.com/dantum150/web-larek-frontend.git

# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Webpack

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- src/pages/index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/index.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

или

```
yarn
yarn start
```
## Сборка

```
npm run build
```

или

```
yarn build
```

Проектная работа: WEB-ларёк

Реализация Проекта Web-ларёк. 
Используемый стек: 
-TypeScript, HTML, SCSS 
-Для сборки используется Web pack.  
-Инструменты линтинга и форматирования не подключены

Задачи:
-спроектировать собственную реализацию MVP-архитектуры веб-приложения;
-задокументировать спроектированную архитектуру;
-реализовать базовый код;
-реализовать на основе базового кода компоненты и модели данных приложения;
-собрать всё вместе.

Функциональные требования (требования описывающие принцип работы интернет-магазина)
Требоавния будут касаться следующего:
-Главной страницы 
-Просмотр товаров 
-Оформление товара
-Требование ко всем страницам 

Cписок вопросов, которые по заданию должна раскрывать документация
1. Из каких основных частей состоит архитектура проекта? (Это могут быть данные, отображения, экраны и так далее.)
2. Зачем нужны эти части, какие функции они выполняют?
3. Как части взаимодействуют?
4. Какие данные используются в приложении? Можете записать конкретные типы данных или интерфейсов, пояснив их функции.
5. Из каких компонентов состоит приложение?
6. Как реализованы процессы в приложении? Через события или как-то иначе, например: с помощью единого контроллера или promise-based flow, как в готовом кейсе в предыдущем спринте.

---

### 1. Из каких основных частей состоит архитектура проекта?

 Данный проект состоит(в ключевой папке components) из:

Папки base (базовый компонет отвечающий за внутренюю логику, (запросы на сервер(связь с backend), сохрание данных и тд.))
-api.ts (работа с сервером, включающая функции по get и post запросу)
-events.ts (данный файл включает взаимодесйтие с обработчиками(сохрание обработчиков, удаление, прослушивание))
-localstorage.ts (сохранение данных, которые сохраняются при перезагрузке страницы или закрытии и повторном открытии браузера.)
-validation.ts (работа с валидацией, проходит все формы и в каждой форме находит характерные инпуты и кнопки и создает функционал в приводе в инпуты кнопка меняется )

Папки buisness (данные приложения, работа с этими данными(хранение, удаление, обработка и тд. ))
-basket.ts (реализация данных адресованых в корзину)
-order.ts (работа направленая с заказом)
-productList.ts (работа с продуктом)

Папки Ui (работа с HTML, пользовательский интрефейс)
-BasketUI.ts
-catalogUI.ts
-contacts.ts
-GeneralUI.ts
-modalUI.ts
-OrderUI.ts

---

### 2. Зачем нужны эти части, какие функции они выполняют?
Систематизация работы по каждой из частей архитиктуры 
<!-- Функциии классов описываются ниже -->

___

### 3. Как части взаимодействуют?

Идет взаимодейсвтие классов (один класс может попасть в другой класс )
Пример: BasketUI 
сonstructor(public modal: AllModal, public basket: Basket) принемает класс modal и класс basket. 
BasketUi может взаимодействиать между двумя классами потому что мы их занесли в конструктор. 
Может реализовываться вызов функции из одного класса в рамках дургого класса.

___

### 4. Какие данные используются в приложении? Можете записать конкретные типы данных или интерфейсов, пояснив их функции.
-Объекты продуктов 
-Объекты заказа 

Все то что идет во взаимодейсвтии с backend и есть необходимые данные (работа с продуктами и заказами)

___

### 5. Из каких компонентов состоит приложение?

три основных типов компоентов: 
-внутрення(глубинная) логинка
-данные приложения
-отрисовка данных (пользовательский интерфейс)

___

### 6. Как реализованы процессы в приложении?

-взаимосвязь с бэкендом 
-устанвока и удуаление обработчиков 
-сохранение и получать данные из локального хранилища 

___

#### Класс Api

__Поле baseUrl__
Нужно для сохранние базовой части URL, чтобы не дублировать URL при кажорм новом запросе (Пример: для работы с продуктами ссылаемся на URL с продуктами, для работы с заказом на URL с закзами ).
Берется основной URL(базовый), который не изменчив(по отношению к элементам из backend), а get и post запрос указываем (в конце) для той или иной задачи в рамках проекта.

__Поле options__
Это объект настроек запроса в частности поле method со значением типа запроса (get, post, delete), headers и поле body в которе попдает объект заказа, который мы должны отправить на сервер

Конструктор (приравнивается к одноименным ключам, чтобы делать фетч запросы)

Конструктор класса, который принимает два параметра: baseUrl типа string и options типа RequestInit (по умолчанию пустой объект).

Внутри конструктора происходит инициализация свойств класса: baseUrl устанавливается равным переданному значению, а options формируется объект, который содержит headers с ключом 'Content-Type' и значением 'application/json'.

Если в переданных options есть свойство headers, то оно распыляется (spread) в новый объект, чтобы сохранить другие заголовки, если они были переданы.
Таким образом, конструктор устанавливает базовый URL и опции для будущих запросов в классе.

Защищенный метод ___handleResponse___, который принимает Response и возвращает Promise, который разрешается в объект.
Внутри метода проверяется свойство ok объекта Response (в связи с булевым правилом от if). Если ответ от сервера успешный (response.ok равен true), то возвращается результат вызова метода .json() для получения данных в формате JSON.
Если ответ от сервера неуспешный(false), то также вызывается метод .json() для получения данных в формате JSON. Затем происходит обработка данных: если в полученных данных есть свойство error, то создается исключение с текстом ошибки из данных, иначе используется текст статуса ответа (response.statusText).

Таким образом, метод handleResponse обрабатывает ответ от сервера, возвращая либо данные в формате JSON в случае успешного ответа, либо создавая исключение с текстом ошибки в случае неуспешного ответа.

___get___ с одним параметром uri типа string. Внутри функции происходит вызов fetch(с помощью данной функции можно отправлять сетевые запросы на сервер а в частности как получать, так и отправлять данные) для отправки GET запроса на указанный uri, используя базовый URL и опции, переданные в объекте. После этого выполняется метод .then для обработки полученного ответа с помощью функции handleResponse

___post___ с тремя параметрами: uri типа string, data типа object и method типа ApiPostMethods по умолчанию равный 'POST'. 
Внутри функции происходит вызов fetch для отправки POST запроса на указанный uri, используя базовый URL и опции, переданные в объекте. 
Также указывается метод запроса (POST или другой, если указан явно) и данные в формате JSON, переданные в теле запроса. 
Затем выполняется метод .then для обработки полученного ответа с помощью функции handleResponse.


#### Класс App

Конструктор класса:

Принимает экземпляры других классов, необходимых для работы приложения, в качестве параметров.

Эти классы включают AllModals, BasketUi, Validation, ProductList, CatalogUI, Api, Basket, OrderUI и Order.

___Метод initApp:___

Вызывается для инициализации приложения.
Инициализирует валидацию форм с помощью validation.initValidation().
Инициализирует корзину с помощью basketUi.initBasket().
Устанавливает обработчики событий для закрытия модальных окон с помощью modal.setCloseListeners().
Вызывает метод getProducts() для получения списка продуктов.
_____Метод getProducts:_____
Использует api.get() для получения списка продуктов с сервера.
При успешном получении данных:
Сохраняет полученные продукты в productList.products.
Вызывает catalog.renderCatalog() для отображения каталога продуктов, передавая функцию обратного вызова для открытия модального окна карточки продукта.
При ошибке выводит сообщение в консоль.
___Метод openCardModal:___
Принимает идентификатор продукта id в качестве параметра.
Использует api.get() для получения данных о продукте с сервера.
При успешном получении данных:
Сохраняет выбранный продукт в basket.selectedProduct.
Вызывает modal.setModal() для обновления содержимого модального окна карточки продукта.
Открывает модальное окно карточки продукта с помощью modal.openModal().
___Метод createOrder:___
Использует api.post() для отправки заказа на сервер, передавая данные заказа с помощью order.returnOrder().
При успешном создании заказа:
Обновляет текст описания успешного оформления заказа в orderUI.successDiscription.

Открывает модальное окно успешного оформления заказа с помощью modal.openModal().
Сбрасывает корзину с помощью basketUi.resetBasket().
При ошибке выводит сообщение об ошибке в консоль.

 Класс App служит точкой входа в приложение, инициализируя и координируя работу различных компонентов, таких как модальные окна, корзина, каталог продуктов и оформление заказа.


#### Класс Form 
Данный класс объединяет ___формы___ у orderModal и contactsModal
Контрукторе происходит наполнение соответсвующими тегами
Данный класс имеет функцию setSubmitHandler которая принемает в себя тег form и имеет колбек onSubmit

На тег form вешается обработчик события __submit__  и происходит вызов функции __onSubmit()__ 


#### Класс LocalStorage 
Класс реализующий хранение какие-либо пользовательских настройк клиента, служебную информацию, различные изменения состояния в процессе взаимодействия со страницей, например, данные заполненных полей формы, чтобы они не удалялись после обновления веб-страницы.
В данном проекте это реализуется в модальных окнах включающие в себя способе оплаты(онлайн или при получении ), инпуты(адрес доставки, email, телефон)

 класс ___LocalStorage___ с двумя методами: get и set.
>1. Метод get(key: string): 
   - Получает значение из локального хранилища по указанному ключу.
   - Проверяет, есть ли данные с указанным ключом в локальном хранилище.
   - Если данные есть, то преобразует их из JSON в объект и возвращает результат.
   
>2. Метод set Value(key: string, value: Value): 
   - Принимает ключ и значение, которое нужно сохранить в локальном хранилище.
   - Преобразует значение в формат JSON.
   - Устанавливает значение в локальное хранилище по указанному ключу.

Таким образом, класс LocalStorage предоставляет функциональность для работы с локальным хранилищем браузера, позволяя получать и устанавливать данные по ключу.

Конструктор(отсутсвует)

#### Класс Validation 
Реалзиация валидации форм  (в частности работа с полями адреса, email, телефона)
Навешивание на кнопку disabled в случае отсутсвия в массиве корзины продуктов

 Класс Validation, содержит методы для валидации формы на веб-странице:
- Конструктор класса принимает объект Order и сохраняет его в свойстве order.
- Метод hasInvalidInput(inputList: HTMLInputElement): 
   - Проверяет, есть ли невалидные данные в переданном списке HTMLInputElement.
- Метод toggleButtonState(inputList: HTMLInputElement, buttonElement: HTMLButtonElement): 
   - В зависимости от наличия невалидных данных в inputList или отсутствия информации о платеже в объекте Order, изменяет доступность кнопки buttonElement.
- Метод setEvenListener(formElement: HTMLFormElement): 
   - Устанавливает слушатели событий для элементов формы: обработчики для ввода в input и клика по кнопкам оплаты.
- Метод initValidation(): 
   - Инициализирует валидацию для всех форм на странице, устанавливая слушатели событий для каждой формы.
- Метод isValid(inputElement: HTMLInputElement, errorElement: HTMLSpanElement): 
   - Проверяет валидность введенных данных в inputElement и отображает сообщение об ошибке при необходимости.
- Методы showInputError и hideInputError используются для отображения и скрытия сообщения об ошибке в элементе errorElement.

Таким образом, класс Validation предоставляет функциональность для валидации форм на веб-странице и управления их состоянием.

#### Класс Basket (работа с данными массива корзины)

Поле basketItems (товары которые были добавлены в массив корзины )
Поле selectedProduct (направленое добавление товара в корзину, происходит сохрание из backend объекта (push объекта в массив))

Осуществялется работа по:
-добавлению данных в корзину,за счет функции addBasket()
-удаление данных из корзины, за счет функции removeBasket()
-установка счетчика в лого-html о колличестве товаров в массиве корзины за счет функции  showBasketCounter()
-подсчет общей суммы за все товары положеные в массив корзины за счет функции calculateTotalPrice()
-очистка всего массиыва корзины по окончания покупки(офрмления заказа) за счет функции cleanBasket()

Конструктор включает в себя работу с двумя параметрами (modal типа Allmodal, basket типа Basket)

 Класс Basket, который отвечает за управление товарами в корзине на веб-странице:
- Свойства класса:
   - basketItems: массив объектов Product, представляющий товары в корзине.
   - selectedProduct: текущий выбранный товар для добавления в корзину.
   - storage: объект LocalStorage для сохранения данных о товарах в локальном хранилище.

- Конструктор класса:
   - Принимает объект LocalStorage и инициализирует пустой массив basketItems.

- Метод addBasket():
   - Добавляет выбранный товар в корзину, если его еще нет в списке товаров.
   - Сохраняет обновленный список товаров в локальное хранилище.

- Метод removeBasket(productID: string):
   - Удаляет товар из корзины по его идентификатору.
   - Обновляет список товаров в локальном хранилище.

- Метод showBasketCounter():
   - Возвращает количество товаров в корзине.

- Метод calculateTotalPrice():
   - Вычисляет общую стоимость всех товаров в корзине.

- Метод cleanBasket():
   - Очищает корзину, удаляя все товары из массива.

Класс Basket предоставляет функциональность для управления товарами в корзине, включая добавление, удаление, отображение количества товаров и подсчет общей стоимости.


#### Класс Order осуществляет работу с заказаом товара(ов)

Класс Order состоит из следующих частей:
___Свойства класса___
- payment: строковое свойство, предназначенное для хранения информации об оплате заказа.
- address: строковое свойство, предназначенное для хранения адреса доставки заказа.
- email: строковое свойство, предназначенное для хранения email-адреса клиента.
- phone: строковое свойство, предназначенное для хранения номера телефона клиента.

___Конструктор класса___

Конструктор класса принимает один параметр __basket__ типа Basket и инициализирует свойство basket класса Order.
В конструкторе также инициализируются свойства payment, address, email и phone пустыми строками.

___Методы класса___

- __setParam(key: string, value: string)__
   - Метод setParam принимает два параметра: key типа string и value типа string.
   - Внутри метода происходит динамическое присваивание значения value свойству класса, соответствующему ключу key.
    - Используется синтаксис this[key] = value для динамического доступа к свойствам класса.

- __returnOrder()__

   - Метод returnOrder возвращает объект, содержащий информацию о заказе.
   - Объект содержит следующие свойства:
      - payment: значение свойства payment класса Order.
      - address: значение свойства address класса Order.
      - email: значение свойства email класса Order.
      - phone: значение свойства phone класса Order.
      - items: массив идентификаторов продуктов в корзине, полученный с помощью метода map из свойства basketItems объекта basket.
      - total: результат вызова метода calculateTotalPrice() объекта basket, возвращающий общую стоимость заказа.

В целом, данный код определяет класс Order, который хранит информацию о заказе, включая корзину с товарами, способ оплаты, адрес доставки, email и номер телефона клиента. Класс предоставляет методы для установки параметров заказа и получения объекта с информацией о заказе.

#### Класс ProductList 
Реализует массив продуктов (хранит инфомрацию о массиве)
Функция getProducts() которая позволяет получить массив продуктов.
Констуктор функции осуществляет поплнение в поле products (продуктов из backend).

В частности стоит отметить использование interface способ типизации объектов (описание того что есть в объекте)

__Интерфейс Product__

Интерфейс Product определяет структуру объекта, представляющего продукт. Он состоит из следующих свойств:

 - id: строковое свойство, представляющее уникальный идентификатор продукта.
- description: строковое свойство, содержащее описание продукта.
- image: строковое свойство, содержащее URL изображения продукта.
- title: строковое свойство, содержащее название продукта.
- category: строковое свойство, определяющее категорию продукта.
- price: числовое свойство, представляющее цену продукта.

Интерфейс Product используется для описания типа объектов, представляющих продукты, и обеспечивает единообразие структуры данных.

Класс ___ProductList___

Класс ProductList предназначен для управления списком продуктов. Он состоит из следующих частей:
___Свойства класса___

   - products: массив объектов типа Product, представляющий список продуктов. По умолчанию массив инициализируется пустым массивом [].

___Конструктор класса___

 - Конструктор класса принимает необязательный параметр products типа Product[], который по умолчанию равен пустому массиву [].
- В конструкторе происходит инициализация свойства products переданным массивом продуктов или пустым массивом, если параметр не передан.

В целом, данный код определяет интерфейс Product для описания структуры продукта и класс ProductList для управления списком продуктов. Класс ProductList позволяет создавать экземпляры с начальным списком продуктов и получать текущий список продуктов.


__Класс BasketUi__

Поле ___addProductButton___ (добавление товара)
Поле ___basketList___ (работа со списком товара в корзине)
Поле ___basketCounter___ (работа с тележкой)
Поле ___totalPrice___ (общая сумма)
Поле ___orderButton___ (офрмление заказов )

Конструктор (включает в себя работу с параметрами modal типа Allmodal, basket типа Basket)
получаем следующий ряд тегов для дальнейшегов заимодействия:
-тега ul списка товаров добавленых в корзину ('.basket__list')
-тега button кнопки добавления товара в массив-товаров коризны ('.card__button')
-тега span иконка продуктовой тележки(корзины) ('.header__basket-counter')
-тега span  вывод суммы (указнных в массиве корзины) ('.basket__price')
-тега button кнопка офомрления в корзине ('.basket__button')

Вызывается конструктор родительского класса Render.
Инициализируются свойства класса, выбираются необходимые элементы из модальных окон.
Добавляются обработчики событий на кнопку "Добавить в корзину" и кнопку оформления заказа.

Включает в себя методы:

___renderBasketItems()___ отрисовка товара в корзине 
- очищение модального окна (для корзины)
- на основе массива товаров создается отдельный элемент и добавляет в тег UL

___createBasketItems()___ создание товара в корзине 
- приводим теги к переменным 
- создается путем компанации

___changeBasketCounter()___ счетчик товаров в корзине
 - за счет конкатенации опирается на вызов функции showBasketCounter() показывающая колличество товаров в корзине

___showCalculateTotalPrice()___
- Ведет подсчет суммы опирается на вызов функции calculateTotalPrice()

___resetBasket()___
- Очищается корзина с помощью метода cleanBasket класса Basket.
- Инициализируется корзина с помощью метода initBasket.

___toggleDisableButton()___
- Реализация неакивной кнопки за счет проверки на наличие товара в корзине. 
- Навешивание disabled в случае если массив корзины равен 0

___initBasket()___
- Вызываются методы renderBasketItems, changeBasketCounter, toggleDisableButton и showCalculateTotalPrice для инициализации корзины.

BasketUi, отвечает за управление пользовательским интерфейсом корзины. Он взаимодействует с классами AllModals, Basket и Render для отображения и обновления корзины, а также обработки событий, связанных с корзиной.

#### Класс CatalogUI работа с карточками в мэйне.
Поле main представляет с собой пустое поле куда будет подгружаться карточки
Поле cardTemplate тег тепмлейт из котрого выгружаем карточки 

Конструктор 
Направлен на присовение тега к переменной 
- тега main главного поля показа товаров ('.gallery')
- тега template описывающий модальное окно ('#card-catalog')

Вызывается конструктор родительского класса Render.
Инициализируются свойства класса, выбираются необходимые элементы из DOM.

renderCatalog (отрисовка катлога)  осуществляет работу с переменными (products: Product[], funcClick: (productId: string) => void)

renderCatalog(products: Product[], funcClick: (productId: string) => void)
Вызывается метод renderList родительского класса Render для рендеринга списка карточек продуктов.
Для каждого продукта вызывается метод createCard для создания карточки.
Для каждой карточки добавляется обработчик события клика с помощью метода addCardClick.

createCard() (отрисовка картчоки)
- присваивает к тегам переменные
- которые буду заполнять модальное окно содержимом (id,category,title,price, description)

___getCategoryClass(category: string)___

Возвращает класс CSS для категории продукта.

___setProductPrice(price: number)___

Форматирует цену продукта в строку с добавлением единицы измерения.

 Класс CatalogUI,  отвечает за отображение каталога продуктов. Он взаимодействует с классом Render для рендеринга списка карточек продуктов и обработки событий клика на карточки. Класс предоставляет методы для создания карточек продуктов и форматирования цены.

#### Класс ContactsUI  форма, где пользователь должен ввести свои контакты для заказа

класс ContactsUI, отвечает за взаимодействие с пользовательским интерфейсом для ввода контактных данных в модальном окне.

Класс состоит из следующих частей:
   - Свойство contactsInputs: Это NodeListOf<HTMLInputElement>, которое будет хранить список всех полей ввода контактных данных в модальном окне.
   - Конструктор класса: Он принимает три параметра:
      - modal: Экземпляр класса AllModals, который отвечает за управление модальными окнами.
      - order: Экземпляр класса Order, который представляет заказ.
      - onSubmit: Функция обратного вызова, которая будет вызываться при отправке формы.
      
- Инициализация contactsInputs: В конструкторе происходит инициализация свойства contactsInputs. Оно получает список всех полей ввода контактных данных в модальном окне с помощью querySelectorAll('.form__input').
 - Обработчики событий для полей ввода: Для каждого поля ввода в contactsInputs устанавливается обработчик события input. При вводе данных в поле вызывается анонимная функция, которая:

      - Получает ссылку на элемент, вызвавший событие, с помощью event.target as HTMLInputElement.
      - Вызывает метод setParam класса Order, передавая в качестве параметров имя поля (target.name) и введенное значение (target.value).
- Обработчик события submit для формы: Для модального окна с контактными данными устанавливается обработчик события submit. При отправке формы вызывается анонимная функция, которая:

- Вызывает метод preventDefault() для предотвращения стандартного поведения формы.
- Вызывает функцию обратного вызова onSubmit, переданную в конструктор класса.

 Происходит связывание полей ввода контактных данных в модальном окне с объектом заказа (Order) и обработку события отправки формы, вызывая переданную функцию обратного вызова.


#### Класс AllModals

Класс AllModals, который расширяет класс Render. Он отвечает за управление модальными окнами в приложении.

Класс состоит из следующих частей:

- Свойства класса:
      - modal: Элемент DOM, представляющий контейнер для всех модальных окон.
      - modalContent: Элемент DOM, представляющий содержимое модального окна.
      - modals: Список всех модальных окон в приложении.
      - cardModal: Элемент DOM, представляющий модальное окно для просмотра карточки товара.
      - basketModal: Элемент DOM, представляющий модальное окно для корзины.
      - orderModal: Элемент DOM, представляющий модальное окно для оформления заказа.
      - contactsModal: Элемент DOM, представляющий модальное окно для ввода контактных данных.
      - successModal: Элемент DOM, представляющий модальное окно для отображения успешного оформления заказа.

- Конструктор класса:

      - Вызывает конструктор родительского класса Render.
      - Инициализирует свойства класса, получая соответствующие элементы DOM с помощью querySelector и querySelectorAll.

- Метод setModal:

      - Принимает объект Product в качестве параметра.
      - Обновляет содержимое модального окна для просмотра карточки товара (cardModal) с использованием данных из объекта Product.
      - Устанавливает текст и классы для категории товара, названия, описания, цены и кнопки.
      - Блокирует кнопку, если цена товара не установлена.

- Метод openModal:
      - Принимает элемент модального окна в качестве параметра.
      - Очищает содержимое modalContent.
      - Добавляет переданный элемент модального окна в modalContent.
      - Добавляет класс modal_active к контейнеру модального окна, чтобы сделать его видимым.

- Метод setCloseListeners:
      - Устанавливает обработчики событий click для закрытия модального окна.
      - При клике на контейнер модального окна или кнопку закрытия удаляет класс modal_active, скрывая модальное окно.

Данный класс отвечает за управление модальными окнами в приложении, включая инициализацию элементов модальных окон, обновление их содержимого, открытие и закрытие модальных окон.


#### Класс OrderUI
Класс состоит из следующих частей:

Свойства класса:

orderButtons: Список кнопок для выбора способа оплаты.

inputAdress: Элемент для ввода адреса доставки.

successDiscription: Элемент для отображения описания успешного оформления заказа.

payments: Объект, определяющий соответствие между выбранным способом оплаты и его описанием.

Конструктор класса:

Принимает два параметра: экземпляры классов AllModals и Order.
Инициализирует свойства класса, получая соответствующие элементы DOM с помощью querySelector и querySelectorAll.
Устанавливает обработчики событий для кнопок выбора способа оплаты, ввода адреса и отправки формы заказа.
Метод selectButton:
Принимает кнопку в качестве параметра.
Снимает выделение со всех кнопок выбора способа оплаты и выделяет переданную кнопку.

Обработчики событий:

Для каждой кнопки выбора способа оплаты устанавливается обработчик события click. При клике на кнопку:
Определяется выбранный способ оплаты.
Устанавливается параметр payment в объекте заказа (Order) с соответствующим значением из объекта payments.
Вызывается метод selectButton для выделения выбранной кнопки.
Для поля ввода адреса устанавливается обработчик события input. При вводе адреса:
Устанавливается параметр address в объекте заказа с введенным значением.
Для формы заказа устанавливается обработчик события submit. При отправке формы:
Предотвращается стандартное поведение формы.
Открывается модальное окно для ввода контактных данных.

Данный класс обеспечивает взаимодействие пользователя с интерфейсом оформления заказа, позволяя выбирать способ оплаты, вводить адрес доставки и переходить к заполнению контактных данных после оформления заказа.

#### Класс Render 
принемает в себя массив, элемент, контейнер, (направлена реализация работы с )
Конструктор отсутсвует

Класс состоит из следующих частей:  

Приватное свойство categoriesClasses:
Объект, определяющий соответствие между категориями и классами CSS для стилей элементов.

Метод renderList:
Принимает массив элементов array, функцию element для создания HTML элемента на основе каждого элемента массива, контейнер container, и опциональную функцию eventListener для добавления обработчика событий.
Для каждого элемента массива:
Создается HTML элемент с помощью функции element.
Если определен eventListener, вызывается для добавления обработчика событий.
HTML элемент добавляется в контейнер.
Метод setProductPrice:
Принимает цену price в виде числа или null.
Возвращает строку, представляющую цену с добавлением текста "синопсов" или "Бесценно", в зависимости от значения цены.
Метод getCategoryClass:
Принимает категорию category в виде строки.
Возвращает соответствующий класс CSS из объекта categoriesClasses для данной категории. Если класс не найден, возвращает пустую строку.

Этот класс предоставляет удобные методы для рендеринга элементов на странице, определения классов для категорий и форматирования цен продуктов. Он может быть использован для упрощения и структурирования процесса создания и отображения элементов веб-приложения.